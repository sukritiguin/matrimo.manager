model Image {
    id        Int      @id @default(autoincrement())
    url       String
    filename  String
    mimetype  String
    size      Int
    createdAt DateTime @default(now())

    Editor Editor[]
}
